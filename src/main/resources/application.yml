server:
  port: 8081
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/db-spring
    username: postgres
    password: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
  liquibase:
    driver-class-name: org.postgresql.Driver
    user: postgres
    password: postgres
    change-log: db/changelog/changelog.xml
  security:
    user:
      name: nw
      password: 12345